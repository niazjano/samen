import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Bli medlem",
};

export default function BecomeMemberLayout({ children }: { children: React.ReactNode }) {
  return children;
}
